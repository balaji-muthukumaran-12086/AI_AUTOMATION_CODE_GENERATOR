# Session Memory — 2026-03-03

## Summary
- **CH-286 Linking Changes**: Completed all 6 test methods — all PASSED ✅ (runs between 11:19–12:39)
- **Framework knowledge sync**: Audited and synced `copilot-instructions.md` with `framework_rules.md` + `framework_knowledge.md`
- **Bug fix**: Added MODULE_TITLE display-ID prefix gotcha to both framework files
- **Clarified demo approach**: Copilot chat (with manual copy+compile+run) is the right demo flow for now

---

## Session Work Log

### 1. Fixed Stale Paths in copilot-instructions.md
Removed hardcoded `/home/balaji-12086/Desktop/Workspace/Zide/...` paths from "Driver & Environment Paths" section.
Replaced with env var names: `FIREFOX_BINARY`, `GECKODRIVER_PATH`, `DEPS_DIR` (all readable from `.env` via `project_config.py`).
Also replaced hardcoded SDP credentials in `RUN_CONFIG` example with symbolic names (`SDP_URL`, `SDP_ADMIN_EMAIL`, etc.).

### 2. Sync Audit: copilot-instructions.md ↔ framework_rules.md + framework_knowledge.md
**Question:** Should content unique to `copilot-instructions.md` flow back into the framework files?

**Findings — already in framework files (no action needed):**
| Section | Location |
|---|---|
| `actions.click()` calls `waitForAjaxComplete()` before | `framework_rules.md` §20.4 |
| `actions.getText()` 3-second timeout | `framework_rules.md` §20.5 |
| `fillInputForAnEntity` skips JSON nulls/booleans | `framework_rules.md` §20.2 |
| `fillDateField` calendar picker behaviour | `framework_knowledge.md` FormBuilder section |
| REST API via browser JS (not HTTP client) | `framework_knowledge.md` §23 (full analysis) |
| Session context: admin→preProcess, user→method | `framework_rules.md` §20.6 |
| `preProcess` silent catch / return false | `framework_rules.md` §20.7 |
| `SolutionAPIUtil` patterns | `framework_knowledge.md` §21, `framework_rules.md` §20.13 |

**Genuinely missing — added:**
- `MODULE_TITLE` locator gotcha: `h1` text **includes display ID prefix** (e.g. `SOL-8 MyTitle`, `CHG-42 MyTitle`).
  `validate.textContent(MODULE_TITLE, "MyTitle")` **FAILS** because of the prefix.
  ALWAYS use `actions.detailsView.verifyTitleInDetailsPage("MyTitle")` which handles the prefix correctly.

### 3. Files Changed (committed ff30553 → pushed to main)
| File | Change |
|---|---|
| `.github/copilot-instructions.md` | Code Generation Rules section added; Driver paths fixed; CH-286 status updated |
| `config/framework_knowledge.md` | MODULE_TITLE display-ID prefix gotcha added to DetailsView method reference |
| `config/framework_rules.md` | §16.1 — replaced misleading `validate.textContent(MODULE_TITLE, ...)` with `verifyTitleInDetailsPage()` |
| `My Memory/2026-03-02.md` | CH-286 status updated to all PASSED |

### 4. CH-286 All Tests Confirmed PASSED (Mar 3 morning)
All 6 methods confirmed via `ScenarioReport.html` existence + "successfully" strings in reports:

| # | Method | Time | Status |
|---|--------|------|--------|
| 1 | `verifyAssociationTabAndAttachOptionsInLHS` | Mar 2 | ✅ PASSED |
| 2 | `verifyAttachParentChangePopup` | 11:19 | ✅ PASSED |
| 3 | `attachParentChangeAndVerifyAssociation` | 12:10 | ✅ PASSED |
| 4 | `detachParentChangeAndVerifyReset` | 12:26 | ✅ PASSED |
| 5 | `verifyAttachChildChangePopup` | 12:29 | ✅ PASSED |
| 6 | `attachDetachChildChangesAndVerifyListView` | 12:39 | ✅ PASSED |

### 5. Demo Flow Decision
For upcoming demo: **Copilot chat flow** (not agent pipeline).

**Demo recipe:**
1. Attach `#file:config/framework_rules.md #file:config/framework_knowledge.md #file:<feature_doc>`
2. Ask: "Generate automation test scenarios. Use two-piece output format with `===== ADD TO:` markers."
3. Copy generated blocks into correct `.java` files
4. Run targeted compile (from copilot-instructions.md §Compilation)
5. Update `run_test.py` → `entity_class` + `method_name` → run `.venv/bin/python run_test.py 2>&1`

**Why not agent pipeline for demo:**
- Local Ollama (`qwen2.5-coder:7b`) generates lower quality code than Claude
- Full pipeline (main.py) needs GPT-4o for demo-quality output (set `OPENAI_API_KEY` in `.env`)
- Copilot chat gives full control and uses Claude quality

---

## Key Decisions Made Today
1. `copilot-instructions.md` is the **Copilot/engineer context** file — project history + quick-ref rules
2. `framework_rules.md` + `framework_knowledge.md` are the **CoderAgent** source files — deeper, complete
3. The two sets should stay in sync for rules that affect code generation
4. For demo, use **Copilot chat** with all 3 files attached manually

---

## Current State of run_test.py
```python
RUN_CONFIG = {
    "entity_class":  "ChangeDetailsView",
    "method_name":   "attachDetachChildChangesAndVerifyListView",  # last run
    "skip_compile":  True,
}
```

## Pending / Next Steps
- Pick next feature document for demo
- Phase 4.5: "Run-Once + UI Copy-Paste" panel in Web UI (see `docs/pipeline-flow.md`)
- Fix 1,350 duplicate `@AutomaterScenario` IDs in Java source (Phase 0.7)
- Consider `OPENAI_API_KEY` setup for higher-quality agent pipeline generation
