# ── Java Selenium modules (managed separately via Mercurial) ──────────────────
# Each user clones their own hg branch via @setup-project — these folders
# must NEVER be committed to git. The wildcard covers any branch name.
AutomaterSelenium/
AutomaterSeleniumFramework/
SDPLIVE_*/
AALAM_*/

# ── Python environment ────────────────────────────────────────────────────────
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/
.eggs/

# ── Secrets & credentials ────────────────────────────────────────────────────
.env

# ── Runtime artifacts ────────────────────────────────────────────────────────
logs/
reports/
generated/

# ── Orchestrator runtime ──────────────────────────────────────────────────────
orchestrator/orchestrator.db
orchestrator/offline_events.jsonl

# ── Node.js (Playwright MCP) ─────────────────────────────────────────────────
node_modules/
package-lock.json
.playwright_mcp.pid

# ── Knowledge base binaries & generated data ──────────────────────────────────
knowledge_base/chroma_db/
knowledge_base/raw/
knowledge_base/scout_snapshots/

# ── Playwright ────────────────────────────────────────────────────────────────
.playwright-mcp/

# ── Code generation backups (rollback safety net) ─────────────────────────────
**/.backups/

# ── VS Code ───────────────────────────────────────────────────────────────────
.vscode/

# ── Archives & logs ───────────────────────────────────────────────────────────
*.zip
*.log

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
SDPLIVE_Linking_Change_Balaji/
SDPLIVE_LINKING_CHANGE_AI_GENERATED/
SDPLIVE_LINKING_CHANGE_AI/
